# SmartCapture 便携版 — 使用说明

## 📦 文件清单

```
SmartCapture-Portable/
├── SmartCapture.exe        ← 双击运行主程序
├── 启动SmartCapture.bat    ← 备用启动脚本
├── README.txt              ← 快速入门
├── 便携版使用说明.md        ← 本文档
├── data/                   ← 数据存储目录（自动创建）
├── locales/                ← 多语言支持（中/英）
└── resources/app/          ← 应用代码
```

---

## 🚀 三步快速开始

### 第一步：拷贝与解压
1. 将 `SmartCapture-Portable.zip` 复制到目标电脑
2. 右键 → **解压到当前文件夹**
3. ⚠️ 不要在压缩包内直接运行，必须先解压！

### 第二步：运行程序
双击 `SmartCapture.exe` 启动。首次启动需几秒初始化，之后自动创建 `data/` 目录。

### 第三步：开始使用
1. **剪贴板捕获**：复制任何文本，应用自动检测并解析
2. **截图捕获**：点击「📷 截图」→ 框选区域 → 选择处理方式
3. **手动粘贴**：点击工具栏「粘贴」按钮

---

## ✨ 功能速查

### 待办任务
- **新建**：点击「➕ 新建任务」手动创建
- **截图存待办**：截图 → 选择「📌 直接存为待办」（无需 AI）
- **多提醒**：每个任务可独立设置多个提醒时间
- **截图预览**：任务卡片显示缩略图，点击可放大预览
- **筛选**：全部 / 今天 / 进行中 / 已完成
- **优先级**：高/中/低三级标记

### 截图功能（两种处理方式）
- **📌 直接存为待办**：不识别文字，截图作为附件保存到待办事项
- **🤖 AI 智能识别**：调用 AI 接口识别截图中的文字内容（需配置 API Key）

### 日历视图
- 月视图展示任务分布（跑马灯显示事件摘要）
- 点击日期查看当天所有待办的完整内容
- 事件卡片显示：标题、时间、优先级、截图缩略图

### 剪贴板捕获
- 自动监控剪贴板（复制 ≥2 字符触发）
- 识别文本中的时间、日期、地点
- 一键转为待办任务

### 快捷键

| 功能 | 快捷键 |
|------|--------|
| 区域截图 | `Ctrl+Shift+S` |
| 显示/隐藏窗口 | `Ctrl+Shift+T` |
| 快速区域截图 | `Ctrl+Shift+Q` |

---

## 💾 数据管理

### 数据位置
```
SmartCapture-Portable/
├── SmartCapture.exe
└── data/
    └── smartcapture.db    ← 所有数据在这里
```

数据完全跟随 EXE，移动文件夹即迁移数据。

### 备份与恢复
- **直接备份**：复制 `data/smartcapture.db` 到安全位置
- **应用导出**：设置 → 导出数据 → JSON 文件
- **应用导入**：设置 → 导入数据 → 选择 JSON 文件

---

## 🔧 AI 配置（可选）

1. 打开应用 → **设置** → **AI 解析配置**
2. 填入你的 API 密钥
3. 点击「🔌 测试连接」验证 API 是否可用
4. 如果显示「余额不足」，请前往 API 平台充值

> 💡 不配置 AI 也完全可用！截图可直接存为待办，无需 AI 参与。

---

## ❓ 常见问题

### Q: 提示「Windows 已保护你的电脑」？
点击 **「更多信息」→「仍要运行」**。便携版无代码签名证书。

### Q: 截图后选择「直接存为待办」，待办卡片看不到截图？
截图数据保存在数据库中，如果卡片未显示截图：
1. 确认是最新版本（v1.0+）
2. 尝试重启应用
3. 点击任务卡片打开详情弹窗查看截图

### Q: AI 识别显示「余额不足」？
你的 AI API Key 余额已用完，请前往对应平台充值或更换 Key。

### Q: 如何彻底退出？
右键系统托盘图标 → 点击「退出」。关闭主窗口不会退出程序。

### Q: 能否放到 U 盘运行？
**可以。** 拷贝整个文件夹到 U 盘，插入后直接运行 `SmartCapture.exe`。

### Q: 数据丢失了怎么办？
- 检查 `data/` 目录是否存在 `smartcapture.db`
- 有备份可直接放回或通过「导入数据」恢复
- 应用启动会自动创建新数据库

---

## 🛡️ 安全与隐私

1. 便携版**无自动更新**机制
2. 所有数据**仅存储本地**，不上传任何服务器
3. 剪贴板监控需用户主动开启
4. AI 解析请求直接发送到你配置的 API 服务商，不经过第三方

---

## 🔧 技术参数

| 项目 | 规格 |
|------|------|
| 运行时 | Electron 28 |
| 数据库 | SQLite (sql.js 纯 JS) |
| 前端 | 原生 HTML/CSS/JS |
| 打包体积 | ~120 MB |
| 数据格式 | SQLite + JSON 导入导出 |
| 语言支持 | 简体中文 / English |

---

© SmartCapture v1.0 — 由 King with Trae 制作 · 让想法不再溜走