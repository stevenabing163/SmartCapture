# SmartCapture 智能待办捕获工具 — 使用说明

## 📦 安装与启动

### 方式一：便携版（推荐）
1. 将 `SmartCapture-Portable.zip` 解压到任意目录
2. 双击 `SmartCapture.exe` 即可运行
3. 数据保存在 EXE 同级的 `data/` 目录下

### 方式二：从源码构建
```bash
# 前提：已安装 Node.js 18+
cd ToDoList
npm install
npm start          # 开发模式
npm run build:portable  # 打包便携版到 dist/ 目录
```

---

## 🖥️ 界面概览

```
┌──────────────────────────────────────────────────────┐
│  📋 待办任务  │  📅 日历视图  │  🎯 捕获中心  │  ⚙️ 设置 │  ← 侧边栏
├──────────────────────────────────────────────────────┤
│  🔍 搜索任务                                        │
│  📷 截图  📋 粘贴  ➕ 新建任务                       │
├──────────────────────────────────────────────────────┤
│  全部  今天  进行中  已完成                          │  ← 筛选
├──────────────────────────────────────────────────────┤
│  ☐ 任务标题 1                                       │
│    描述 / 标签 / 截止日期                           │
│  ☑ 任务标题 2 (已完成)                              │
└──────────────────────────────────────────────────────┘
```

---

## ✨ 核心功能

### 1. 任务管理
- **新建任务**：点击「➕ 新建任务」或使用快捷键
- **编辑任务**：双击任意任务卡片
- **完成任务**：点击任务前的复选框
- **删除任务**：编辑任务 → 点击「删除」
- **搜索任务**：顶部搜索框支持标题/描述模糊搜索
- **筛选任务**：切换「全部/今天/进行中/已完成」标签
- **多提醒设置**：每个任务可独立设置 0分钟/5分钟/15分钟/30分钟/1小时/1天等多个提醒时间

### 2. 剪贴板监控
- 程序自动监听系统剪贴板
- 复制文本（≥2个字符）后弹出解析窗口
- 自动分析内容，提取任务信息
- 在「捕获中心」或「设置」页面可随时开关

### 3. 截图捕获
- **全屏截图**：点击工具栏「📷 截图」按钮或 `Ctrl+Shift+A`
- **区域截图**：点击「区域截图」或 `Ctrl+Shift+S` → 拖动鼠标框选
- 截图进入解析流程，可保存为待办任务

### 4. 日历视图
- 按日期查看任务分布
- 点击日期查看当天任务
- 直观展示任务时间线

### 5. 数据管理
- **导出数据**：设置 → 导出数据 → 保存为 JSON 备份
- **导入数据**：设置 → 导入数据 → 选择 JSON 备份文件
- **清空数据**：设置 → 清空所有数据（⚠️ 不可恢复）

### 6. 系统托盘
- 程序默认最小化到系统托盘运行
- 右键托盘图标可：截图、显示窗口、退出
- 关闭主窗口后程序仍在后台运行

### 7. 手机访问 (PWA)
- 手机和电脑在同一 WiFi 下
- 手机浏览器访问设置页显示的局域网地址
- 可添加到主屏幕，离线使用

---

## ⌨️ 全局快捷键

| 快捷键 | 功能 |
|--------|------|
| `Ctrl+Shift+A` | 全屏截图 |
| `Ctrl+Shift+S` | 区域截图 |
| `Ctrl+Shift+T` | 显示/隐藏主窗口 |
| `Ctrl+Shift+Q` | 快速区域截图 |

---

## 📤 迁移到其他电脑

### 便携版（推荐）
1. 将整个 `SmartCapture-Portable` 文件夹复制到 U 盘或云盘
2. 在另一台 Windows 电脑上双击 `SmartCapture.exe` 即可运行
3. 数据跟随 EXE，移动文件夹即迁移数据

### 完整数据迁移
1. 在旧电脑：设置 → 导出数据 → 保存 JSON 文件
2. 在新电脑：运行 SmartCapture → 设置 → 导入数据 → 选择 JSON 文件
3. 所有任务和设置将完整迁移

---

## 🛠️ 常见问题

**Q: 剪贴板监控没有反应？**
A: 请确认已在「捕获中心」或「设置」中开启剪贴板监控。复制内容需 ≥2个字符。

**Q: 截图快捷键没反应？**
A: 首次启动后快捷键才会注册。如果与其他软件快捷键冲突，可在设置中修改。

**Q: 如何彻底退出程序？**
A: 右键系统托盘图标 → 点击「退出」。关闭主窗口不会退出程序。

**Q: 数据存在哪里？**
A: 便携版：EXE 同级 `data/smartcapture.db`；开发版：项目根目录 `data/smartcapture.db`

**Q: 支持哪些系统？**
A: Windows 10/11 64位。手机需 iOS 12+ 或 Android 7+。

**Q: 手机无法访问 Web 服务？**
A: 检查防火墙是否放行 3000 端口，确认手机和电脑在同一 WiFi。

---

## 📁 项目结构

```
ToDoList/
├── main.js                 # Electron 主进程入口
├── preload.js              # 预加载脚本（IPC 桥接）
├── package.json            # 项目配置
├── build-portable.js       # 便携版打包脚本
├── src/
│   ├── core/
│   │   ├── store.js        # 数据库操作（sql.js）
│   │   ├── parser.js       # 内容解析器（规则+AI）
│   │   ├── screenshot.js   # 截图捕获
│   │   ├── clipboard.js    # 剪贴板监听
│   │   ├── hotkey.js       # 全局快捷键
│   │   ├── tray.js         # 系统托盘管理
│   │   └── web-server.js   # HTTP 服务（手机访问）
│   └── ui/
│       ├── index.html      # 主界面
│       ├── renderer.js     # 渲染进程逻辑
│       ├── styles.css      # 样式表
│       ├── manifest.json   # PWA manifest
│       ├── service-worker.js # Service Worker
│       ├── region-selector.html  # 区域选择器
│       └── region-preload.js     # 选择器预加载
└── data/                   # 本地数据库目录
```

---

© SmartCapture v1.0 — 由 King with Trae 制作 · 让想法不再溜走